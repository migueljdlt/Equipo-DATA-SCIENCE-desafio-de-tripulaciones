# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================
PORT=3000
NODE_ENV=development

# ============================================
# BASE DE DATOS POSTGRESQL
# ============================================
DB_HOST=tu_host_aqui
DB_PORT=5432
DB_NAME=tu_base_de_datos
DB_USER=tu_usuario
DB_PASSWORD=tu_password
DB_SSL=require

DATABASE_URL=postgresql://usuario:password@host:puerto/database?sslmode=require

# ============================================
# OLLAMA
# ============================================
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b
OLLAMA_TEMPERATURE_SQL=0.1
OLLAMA_TEMPERATURE_TEXT=0.3

# ============================================
# FLASK API
# ============================================
FLASK_API_URL=http://localhost:5000
```

---

## **Estructura final del proyecto**
```
proyecto-final-data/
│
├── .env                    # ⚠️ NO SUBIR A GIT (datos sensibles)
├── .env.example           # ✅ Subir a Git (template)
├── .gitignore             # ✅ Subir a Git
├── package.json
├── server.js
│
└── src/
    ├── config/
    │   ├── database.js    # ✅ Actualizado con process.env
    │   ├── ollama.js      # ✅ Actualizado con process.env
    │   └── constants.js
    │
    └── ...